<Window x:Class="SimpleXmlEditor.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputDialog" Height="180" Width="380"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                   Name="TitleTextBlock"
                   FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Name="Label1Text" VerticalAlignment="Center" Margin="0,0,0,8"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="Input1" Margin="5,0,0,8" Height="28"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Name="Label2Text" VerticalAlignment="Center" Margin="0,0,0,15"/>
        <TextBox Grid.Row="2" Grid.Column="1" Name="Input2" Margin="5,0,0,15" Height="28"/>

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Name="CancelBtn" Content="取消" Width="70" Margin="0,0,10,0" Click="CancelBtn_Click"/>
            <Button Name="OKBtn" Content="确定" Width="70" Click="OKBtn_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
