<Window x:Class="SimpleXmlEditor.FileTypeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择文件类型" Height="160" Width="350"
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        Background="#F7F6FB" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Window.Resources>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Name="PromptText" Text="这个文件是原文还是译文？"
                   FontWeight="Bold" FontSize="14" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Name="SourceBtn" Content="原文" Width="80" Height="32" Margin="0,0,10,0"
                    Click="SourceBtn_Click" IsDefault="True" Style="{StaticResource PrimaryBtn}"/>
            <Button Name="TranslationBtn" Content="译文" Width="80" Height="32" Margin="0,0,10,0"
                    Click="TranslationBtn_Click" Style="{StaticResource PrimaryBtn}"/>
            <Button Name="CancelBtn" Content="取消" Width="80" Height="32" Click="CancelBtn_Click" IsCancel="True" Style="{StaticResource GreyBtn}"/>
        </StackPanel>
    </Grid>
</Window>
